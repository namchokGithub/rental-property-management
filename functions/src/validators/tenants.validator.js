const { AppError } = require("../errors/app-error"); const { ERROR_CODES } = require("../errors/error-codes"); const STATUSES = new Set(["active", "inactive"]); const EMAIL = /^[^\s@]+@[^\s@]+\.[^\s@]+$/; const fail = () => { throw new AppError(400, ERROR_CODES.VALIDATION_ERROR, "Invalid tenant data"); }; const text = (value) => { if (typeof value !== "string") fail(); return value.trim(); };
function map(body, required) { if (!body || typeof body !== "object") fail(); const data = {}; if (required || body.fullName !== undefined) { data.fullName = text(body.fullName); if (!data.fullName) fail(); } for (const key of ["phone", "identificationNumber", "address", "emergencyContactName", "emergencyContactPhone", "notes"]) if (body[key] !== undefined) data[key] = text(body[key]); if (body.email !== undefined) { data.email = text(body.email); if (data.email && !EMAIL.test(data.email)) fail(); } if (required || body.status !== undefined) { data.status = body.status === undefined ? "active" : body.status; if (!STATUSES.has(data.status)) fail(); } if (!Object.keys(data).length) fail(); return data; }
module.exports = { create: (body) => map(body, true), update: (body) => map(body, false), STATUSES };
